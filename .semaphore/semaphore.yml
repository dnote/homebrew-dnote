version: 1.0
name: Apple/Mojave Based Pipeline

agent:
  machine:
    type: a1-standard-4
    os_image: macos-mojave

blocks:
  - name: "Brew test"
    task:
      jobs:
        - name: Test
          commands:
            - brew audit dnote
            - brew tap dnote/dnote
            - brew install dnote
            - brew test dnote
